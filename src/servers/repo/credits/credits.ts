import client from "@api/client";
import { Category } from "../../types/props";

export const creditVideos = async (category: Category, id: string) => {
  // try {
  //   const url = `${category}/${id}/credits`;
  //   const result = await client.get(url);
  //   return result;
  // } catch (error) {
  //   console.error(error);
  //   return null;
  // }
};
