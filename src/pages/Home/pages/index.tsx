import React from "react";
import HomeHeader from "../components/HomeHeader";

const Home = () => {
  return (
    <React.Fragment>
      <HomeHeader />
    </React.Fragment>
  );
};

export default Home;
